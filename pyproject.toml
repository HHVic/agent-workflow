[project]
name = "agent-workflow"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.27.2",
    "openai>=2.38.0",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
